{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "prisma:tasks:generate": "prisma generate --schema=tools/tasks/prisma/schema.prisma",
    "prisma:tasks:migrate": "prisma migrate dev --schema=tools/tasks/prisma/schema.prisma",
    "prisma:auth:generate": "prisma generate --schema=common/auth/prisma/schema.prisma",
    "prisma:auth:migrate": "prisma migrate dev --schema=common/auth/prisma/schema.prisma",
    "prisma:content:generate": "prisma generate --schema=tools/content/prisma/schema.prisma",
    "prisma:content:migrate": "prisma migrate dev --schema=tools/content/prisma/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prettier": "^3.9.5",
    "prisma": "^6.19.3"
  }
}
